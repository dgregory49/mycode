#!/usr/bin/env python3

# Check hostname to see if it's "mtg"

hostname = input("Hostname Value: ")

if hostname.lower() == "mtg":
    print("The hostname was found to be mtg")
    print("hostname matches expected config")

print("Exiting the script.")
